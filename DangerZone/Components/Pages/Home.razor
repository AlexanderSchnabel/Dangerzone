@page "/"
@layout EmptyLayout

<PageTitle>Exit Strategy</PageTitle>

<MudStack AlignItems="@(AlignItems.Center)" Justify="@(Justify.Center)" Style="height: 80vh;">
    <MudImage Src="@("/img/logo.png")" ObjectFit="@(ObjectFit.Contain)"></MudImage>
    <MudButton Variant="@(Variant.Filled)" Color="@(Color.Primary)" Href="@("/events/")">LET'S GO</MudButton>
</MudStack>
