@page "/"
@layout EmptyLayout

<PageTitle>Exit Strategy</PageTitle>

<MudStack AlignItems="@(AlignItems.Center)" Justify="@(Justify.Center)" Style="height: 80vh;">
    <MudButton Variant="@(Variant.Filled)" Color="@(Color.Success)" Href="@("/events")">LET'S GO</MudButton>
</MudStack>
